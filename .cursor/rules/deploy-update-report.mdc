---
description: 生产部署成功后必须写一份 DreamGrid 系统消息体例的更新报告
alwaysApply: true
---

# DreamGrid 部署更新报告

产品名：**DreamGrid**。仅在**生产环境部署成功之后**写；本地重启、commit、push 不写。

## 必须做

1. 在对导演的回复里给出完整报告（可直接复制到「公告管理」）。
2. 同时给出两段纯文本：`标题`、`正文`。
3. 问一句：是否现在发布到站内「系统消息」。导演说发，再发；未说则只给文案。

## 体例（对照站内系统消息）

- 称呼网站为 DreamGrid，勿写仓库名 / 内部任务号（AG239 等）进用户可见正文。
- 语气正式、礼貌、可扫读。用户收益优先，不写实现细节、文件名、SHA（SHA 可另附一行给导演）。
- 结构固定：

```
标题：
(重要通知) DreamGrid {一句话主题}公告

正文：
尊敬的 DreamGrid 用户：

感谢您一直以来对 DreamGrid 的支持与信任。为持续提升创作体验与系统稳定性，我们已于 {YYYY年M月D日} 完成更新。本次更新现已生效。

本次更新带来以下改进：

1. **短标题**：用户能感知的变化（1–2 句）。
2. **短标题**：……

如在使用中遇到任何问题，欢迎随时向我们反馈。

DreamGrid 团队
```

- 改进用编号列表，每条 **加粗短标题** + 一句说明。只写本次实际上线且用户能感到的点；无用户可见变化则不发报告，改为一句告知导演。
- 收尾保持「我知道了」式闭合，不另造口号。
