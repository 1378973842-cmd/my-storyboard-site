# 跨会话交接报告 (Handoff)

- **当前进度**：
  0. **本次会话（未上线）**：复制/下载不再误用工具栏缩小 JPEG 预览，正式地址走 OSS 原图。
  0. **已上线 v1.0.115（deploy @ cd273c4）**：工作区封面/Output 九宫格走缩略图+OSS 直链；复制/下载走签名直链（有 blob 则直接用）；拖入/粘贴上传完板上图不换 src。画笔裁到图内；旋转取消入场放大；工具栏新节点贴原图右侧。备份 `studio-20260908-183436.tar.gz`（10G）。PM2 HTTP 200。
  0. **已上线 v1.0.113（deploy @ d7436d5）**：灯箱/悬停预览走 OSS 签名直链；拖入与画笔上传预签名直传阿里云（失败回退本站）；画笔/裁剪/旋转节点用缩小 JPEG 立刻铺满。备份 `studio-20260903-192508.tar.gz`（8.5G）。PM2 HTTP 200。
  0. **已上线 v1.0.111（deploy @ 8357197）**：应用画笔/裁剪/旋转立刻出新图（blob 先落板，上传/OSS/缩略图后台化）。备份 `studio-20260903-181957.tar.gz`（8.3G）。PM2 HTTP 200。
  0. **已上线 v1.0.110（deploy @ 1733dd5）**：OSS 同源代理。`/uploads` 不再 302 到阿里云；画笔/裁剪/旋转/宫格/截帧/剪辑可在线上用。备份 `studio-20260903-173033.tar.gz`（8.3G）。
  0. **已上线 v1.0.109（deploy @ 6cbf802）**：全景图 360（动作条入口，结果 16:9 拖视角，工具栏截图/重置/全屏/进出预览）；画笔「应用画笔」可点，失败提示挂在工具条上。
  0. **已上线 v1.0.108（deploy @ 2ed3562）**：新视角（箭头指向 / 3D机位，出图=原图+预览构图参考）、重绘（蒙版 Qwen / 框选贴回）、灯箱对比（点击、线对齐、原图/结果角标、对比底图 PNG）、图台收回控制台淡入淡出。2G ECS **未安装** `@huggingface/transformers`（onnxruntime 会 302/OOM）；线上 3D 机位深度预览常为平面，出图仍跟构图参考。
  0. **线上生图不认参考图（已上线 v1.0.107）**：OSS 启用后拖入图只在私有桶、不在本地盘；`/uploads` 一律读盘或 OSS 直读再上传 RH；图片生成节点不再注入「角色造型锁定」（九宫格仍锁）。
  0. **Mx-Shell / 九宫格参考图被挡住（已上线 v1.0.107）**：九宫格参考图折行+区内滚动。
  0. **透明参考图铺白（已上线 v1.0.107）**：生图/生视频/MJ 参考图有 alpha 则铺白再传 RH；扩图仍保留透明。
  0. **阿里云 OSS 素材私有存储（已上线并启用）**：Bucket `dreamgrid-media` @ `oss-cn-guangzhou`。看图/上传走签名直链；画笔/裁剪 canvas 仍同源代理。
  0. **切换项目卡顿优化（已随 v1.0.105 上线）**：骨架屏 + 开板分帧渲染视口外节点。
  1. **画布五项 / 高清放大 / 抠图 / RH 输出预览（已随此前版本上线）**：高清放大/抠图仍待导演实测 RunningHub 真实返回。
  5. **线上**：`deploy` @ `cd273c4`（v1.0.115）。用户入口 `https://dreamgrid.cn`（Caddy 反代 127.0.0.1:3000）。
  6. **拖入/粘贴占位（已上线）**：blob 占位后只换落盘 URL，板上预览不重拉。
  7. **部署版本号（已上线）**：线上 `v1.0.115`。
  8. **扩图已开（只用 RunningHub）**：gpt-image-2 透明通道；待实测生成式边界。
  9. 远端备份：最新 `/root/studio-backups/studio-20260908-183436.tar.gz`（10G）；策略留 3 份。
- **验证状态**：生产仍为 v1.0.115。本地：复制/下载已改走正式原图（裁剪/画笔/旋转新节点不再下到 1280 JPEG）。请导演本地对工具栏新节点点下载，核对分辨率。
- **本地网站**：`http://localhost:3005`。
- **访问**：`https://dreamgrid.cn`（勿用 `http://8.163.127.198:3000`）。
- **流程**：生产部署成功后写 DreamGrid 系统消息体例报告；导演点头后再发站内公告。
- **待办**：复制/下载原图修复可随下轮上线。框选/蒙版/3D机位；日间选中色是否改钴蓝待拍板；高清放大/抠图仍待实测。本地未提交：部署脚本卸 transformers + `package.json` 将其改为 optional。
- **已知未覆盖（需导演决定是否继续）**：
  1. 画布「选择画布」入口页（`.canvas-gate`）仍是炭黑。
  2. Loop 节点内部少数浅色文字白底下略淡。
  3. 历史用户无 `login_events` 回填（仅新登录起有记录）。
  4. 旧画布 JSON 里的本板 logs 不再展示。
  5. 大板仍无视口虚拟化。
  6. 框选融合未移植 PixelRunner stretch + local-mesh。
  7. 线上 3D 机位无服务端 Depth Anything（2G 装不下 onnx）；预览多为平面。
  8. 删除素材/头像只清本地盘，OSS 对象可能残留（不挡功能）。
- **Blockers**：视频剪辑无服务端 ffmpeg；关标签后任务归属仍依赖 sessionStorage；扩图/抠图/高清放大/全景图待实测。

## 新对话开场白（复制给 Agent）

```
继续 gemini-deploy。先读 docs/HANDOFF.md。
线上已是 v1.0.115（deploy @ cd273c4）：封面/Output 缩略图+OSS 直链，复制/下载直链，拖入不二次刷图；画笔裁到图内，旋转不飞镜，工具栏新图贴右侧。
本地未上线：复制/下载勿用工具栏缩小 JPEG，走 OSS 原图。
2G ECS 未装 transformers。框选仍缺 stretch/local-mesh。
按导演下一条继续。
```
