# 跨会话交接报告 (Handoff)

- **当前进度**：
  0. **画笔应用（本次，未部署）**：点「应用画笔」无反应——点击可能被浮标/定位闪藏吃掉，失败提示只进隐藏状态栏。已改为引擎捕获点击、工具条上可见提示、笔迹层对齐预览图。
  0. **全景图 360（未部署）**：结果节点默认 **16:9 拖视角**。退出全景预览收进上方同一条工具栏（与截图/重置/全屏一排），画面右上角另有进出。退出后成 21:9 整图。
  0. **已上线 v1.0.108（deploy @ 2ed3562）**：新视角（箭头指向 / 3D机位，出图=原图+预览构图参考）、重绘（蒙版 Qwen / 框选贴回）、灯箱对比（点击、线对齐、原图/结果角标、对比底图 PNG）、图台收回控制台淡入淡出。2G ECS **未安装** `@huggingface/transformers`（onnxruntime 会 302/OOM）；线上 3D 机位深度预览常为平面，出图仍跟构图参考。部署脚本本地已改：卸掉 transformers 再安装依赖，PM2 空则 `start`。
  0. **线上生图不认参考图（已上线 v1.0.107）**：OSS 启用后拖入图只在私有桶、不在本地盘；`/uploads` 一律读盘或 OSS 直读再上传 RH；图片生成节点不再注入「角色造型锁定」（九宫格仍锁）。
  0. **Mx-Shell / 九宫格参考图被挡住（已上线 v1.0.107）**：九宫格参考图折行+区内滚动。
  0. **透明参考图铺白（已上线 v1.0.107）**：生图/生视频/MJ 参考图有 alpha 则铺白再传 RH；扩图仍保留透明。
  0. **阿里云 OSS 素材私有存储（已上线并启用）**：Bucket `dreamgrid-media` @ `oss-cn-guangzhou`。
  0. **切换项目卡顿优化（已随 v1.0.105 上线）**：骨架屏 + 开板分帧渲染视口外节点。
  1. **画布五项 / 高清放大 / 抠图 / RH 输出预览（已随此前版本上线）**：高清放大/抠图仍待导演实测 RunningHub 真实返回。
  5. **线上**：`deploy` @ `2ed3562`（v1.0.108）。用户入口 `https://dreamgrid.cn`（Caddy 反代 127.0.0.1:3000）。
  6. **拖入/粘贴占位（已上线）**：blob 占位后换真实 URL。
  7. **部署版本号（已上线）**：线上 `v1.0.108`。
  8. **扩图已重开（只用 RunningHub）**：gpt-image-2 透明通道；待实测生成式边界。
  9. 远端备份：最新 `/root/studio-backups/studio-20260901-141650.tar.gz`（7.7G）；策略留 3 份。
- **验证状态**：本会话画笔「应用」二次修复后待导演硬刷新实测。生产仍为 v1.0.108 / `2ed3562`（本次未部署）。
- **本地网站**：`http://localhost:3005`。硬刷新后：画笔涂一笔再点「应用画笔」，应在原图右侧连出带笔迹的新节点；若没画，工具条旁会出现「请先绘制」而不是没反应。
- **访问**：`https://dreamgrid.cn`（勿用 `http://8.163.127.198:3000`）。
- **流程**：生产部署成功后写 DreamGrid 系统消息体例报告；导演点头后再发站内公告。
- **待办**：导演硬刷新后实测画笔涂抹→应用（右侧新节点带笔迹）；全景图 RunningHub 真实返回与 360 预览；框选/蒙版/3D机位；日间选中色是否改钴蓝待拍板；高清放大/抠图仍待实测。本地未提交：部署脚本卸 transformers + `package.json` 将其改为 optional。
- **已知未覆盖（需导演决定是否继续）**：
  1. 画布「选择画布」入口页（`.canvas-gate`）仍是炭黑。
  2. Loop 节点内部少数浅色文字白底下略淡。
  3. 历史用户无 `login_events` 回填（仅新登录起有记录）。
  4. 旧画布 JSON 里的本板 logs 不再展示。
  5. 大板仍无视口虚拟化。
  6. 框选融合未移植 PixelRunner stretch + local-mesh。
  7. 线上 3D 机位无服务端 Depth Anything（2G 装不下 onnx）；预览多为平面。
- **Blockers**：视频剪辑无服务端 ffmpeg；关标签后任务归属仍依赖 sessionStorage；扩图/抠图/高清放大/全景图待实测。OSS 已在生产启用。

## 新对话开场白（复制给 Agent）

```
继续 gemini-deploy。先读 docs/HANDOFF.md。
线上已是 v1.0.108（deploy @ 2ed3562）。
本地未部署：全景图 360 已接（默认 21:9/medium/4k）。2G ECS 未装 transformers；3D 机位深度预览线上常为平面。框选仍缺 stretch/local-mesh。
按导演下一条继续。
```
